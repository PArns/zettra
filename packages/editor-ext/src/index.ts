/**
 * @zettra/editor-ext — BlockNote custom inline content + suggestion plugin building blocks
 * (§4, §8.6). The framework-agnostic, unit-tested core lives here; the client assembles the
 * concrete BlockNote specs and ProseMirror plugins from these definitions.
 */
export * from './reference-primitive';
export * from './triggers';
export * from './auto-detect';
